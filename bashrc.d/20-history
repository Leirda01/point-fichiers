export HISTSIZE=500
export HISTCONTROL=ignoreboth:erasedups
shopt -s histappend
PROMPT_COMMAND=${PROMPT_COMMAND:+$PROMPT_COMMAND; }'history -n; history -w; history -c; history -r'

# vim: filetype=bash
